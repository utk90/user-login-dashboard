const LOGIN = '/login';
const DASH = '/user/:username';
const ALL = '/user/:username/all';
const MOTIVATION = '/user/:username/motivation';
const ERROR = '*';

const Path = {
    LOGIN: LOGIN,
    DASH: DASH,
    ALL: ALL,
    MOTIVATION: MOTIVATION,
    ERROR: ERROR,
}

export default Path;